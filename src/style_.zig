pub const Measurements = struct { margin: Margin = .{}, padding: Padding = .{} };

pub const Margin = struct { left: f64 = 0, right: f64 = 0, top: f64 = 0, bottom: f64 = 0 };

pub const Padding = struct { left: f64 = 0, right: f64 = 0, top: f64 = 0, bottom: f64 = 0 };
